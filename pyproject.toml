[project]
name = "fastapi-first-try"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alex Kostyniuk", email = "kostyniuk7@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.8",
]

[project.scripts]
fastapi-first-try = "fastapi_first_try:main"

[tool.fastapi]
entrypoint = "fastapi_first_try.main:app"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
